{
  "name": "selom-azume-2.0",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sass-dev": "sass --watch --update --style=expanded sass/main.scss:styles.css",
    "sass-prod": "sass --no-source-map --style=compressed sass/main.scss:styles.css",
    "prefix": "postcss --use autoprefixer -b 'last 10 versions' styles.css -o style.prefix.css"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "autoprefixer": "^10.4.13",
    "sass": "^1.56.0"
  },
  "devDependencies": {
    "postcss": "^8.4.18",
    "postcss-cli": "^10.0.0"
  }
}
